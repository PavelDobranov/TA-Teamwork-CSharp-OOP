<Page x:Class="AcademySystem.WpfClient.Views.CourcesListFormView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="CourcesListFormView">

    <Grid>
        <ListView Margin="0,0,-500,0" Name="Cources">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Category" Width="Auto" DisplayMemberBinding="{Binding Category}" />
                    <GridViewColumn Header="StartDateTime" Width="Auto" DisplayMemberBinding="{Binding StartDateTime}" />
                    <GridViewColumn Header="EndDateTime" Width="Auto" DisplayMemberBinding="{Binding EndDateTime}" />
                    <GridViewColumn Header="Description" Width="Auto" DisplayMemberBinding="{Binding Description}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
